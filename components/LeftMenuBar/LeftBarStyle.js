import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const LeftBarWrapper = styled(Box)(({ theme }) => ({
  display: "flex",
  flexDirection: "row",
  backgroundColor: "#313649",
  height: "auto",
}));
